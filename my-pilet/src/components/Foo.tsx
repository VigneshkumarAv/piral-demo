/* import * as React from 'react';

export default ({data}) => {
  return (
    <>
      <h1>Foo Data</h1>
      {data.map((item)=>( // NOT used file
        <ul>
          <li key={item.id}>{item.title}</li>
        </ul>
      ))}
    </>
  );
}; */
